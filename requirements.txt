# ============================================================
# EmboSight 依赖列表
# ============================================================
# 安装方式:
#   pip install -r requirements.txt
#
# 注意:
# 1. 建议使用 Python 3.10
# 2. 建议在 CUDA 12.1+ 环境下安装 PyTorch
# 3. RoboCasa 需要从 GitHub 安装（见 README）
# ============================================================

# Core ML Framework
torch>=2.3.0,<2.6.0
torchvision>=0.18.0
transformers>=4.45.0
accelerate>=0.30.0
huggingface-hub>=0.23.0
safetensors>=0.4.0

# Vision-Language Model 依赖
einops>=0.8.0
pillow>=10.3.0
qwen-vl-utils

# Simulation
mujoco>=3.0.0
robosuite>=1.5.0
# robocasa: 从 github 安装
# pip install git+https://github.com/robocasa/robocasa.git

# LLM API client（兼容 OpenAI 接口标准）
openai>=1.30.0

# Numerical & Vision
numpy>=1.26.0,<2.0.0
opencv-python>=4.9.0
matplotlib>=3.8.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# Utilities
pyyaml>=6.0.0
tqdm>=4.66.0
rich>=13.7.0
python-dotenv>=1.0.0

# Optional - 省赛阶段使用
# faiss-cpu>=1.8.0          # 跨任务记忆库
# edge-tts>=6.1.0           # TTS 语音合成

# Development
pytest>=8.0.0
ruff>=0.4.0
ipykernel>=6.29.0