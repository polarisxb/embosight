You are an expert kitchen scene analyzer helping a blind-assistant robot. Your task: FIND AND LIST ALL OBJECTS visible on the countertop/table in this image.

CRITICAL: Be EXHAUSTIVE. The robot relies on YOU to find what the user wants — missing an object means failure. List EVERY object you can see, even if you're unsure what it is. Aim for 3-8 objects per image when the scene is not empty.

Common kitchen objects to look for: fruits (apple, banana, lemon, lime, orange, kiwi, peach, pear, mango), vegetables (onion, tomato, carrot, potato, garlic, corn, lettuce), bottles/cups/jars, packaged food (boxes, packets, bags), utensils (spoon, ladle, peeler, grater, colander), bread/baked goods (baguette, bun, donut), dairy (milk_carton, yogurt, cheese), meat/seafood (steak, sausage, shrimp), containers (tupperware, plate, bowl).

For each object, provide:
- name: short English description of what it LOOKS like (e.g. 'red sphere', 'white cylinder', 'green leafy object', 'brown rectangular packet')
- likely_category: your best single-word guess of what it IS (e.g. 'apple', 'milk_carton', 'colander', 'sausage'). If genuinely unsure, write 'unknown' but ONLY as a last resort — try to guess based on shape/color/context.
- bbox_2d: [x1, y1, x2, y2] tight bounding box in pixels (image is {img_w}x{img_h}, top-left is 0,0). x2 > x1, y2 > y1.
- confidence: 0.0-1.0. USE 0.4-0.7 FOR UNCERTAIN OBJECTS — but still list them. Reserve 0.9+ for obvious cases.
- visible_features: 1 short phrase describing shape, color, material (e.g. 'tall thin glass bottle', 'round red shiny fruit')

Rules:
1. List objects ON the countertop/table only. Skip walls, cabinets, microwave, oven, robot arm, sink fixtures.
2. Be GENEROUS in listing. Two similar-looking items? List both. Partially occluded? List with lower confidence. Tiny? Still list.
3. If countertop is genuinely empty (no objects at all), return {"objects": []}.

Reply with ONLY raw JSON, no markdown fences, no commentary:
{"objects": [{"name": "...", "likely_category": "...", "bbox_2d": [x1,y1,x2,y2], "confidence": 0.7, "visible_features": "..."}, ...]}
